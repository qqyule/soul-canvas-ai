# Security Policy

感谢你帮助 Soul Canvas 变得更安全。

## 支持范围

当前主要维护 `main` 分支。安全修复会优先合入 `main`，并在需要时发布说明。

## 报告漏洞

请不要在公开 Issue 中披露漏洞细节、攻击载荷、真实密钥或可复现的利用步骤。

推荐方式：

1. 如果 GitHub Security Advisory 可用，请通过仓库的私密安全报告入口提交。
2. 如果不可用，请通过维护者 GitHub 主页提供的联系方式私下沟通。
3. 如果只能创建公开 Issue，请只描述影响范围，不包含利用细节，并注明需要私下跟进。

报告时请尽量包含：

- 受影响的功能或文件。
- 风险等级和潜在影响。
- 安全的复现思路。
- 你建议的修复方向。

## 密钥与环境变量

- 不要提交 `.env.local`、真实 API Key、数据库连接串、S3 凭证或 Worker secrets。
- 前端 `VITE_` 变量会被打包到浏览器代码中；生产环境不要把服务端密钥放在 `VITE_` 变量里。
- 需要保护的第三方 API Key 应放在 Worker、Serverless Function 或后端服务中。
