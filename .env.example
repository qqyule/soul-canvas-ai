# ============================================
# 前端环境变量
# ============================================

# OpenRouter API Key (必填)
# 前往 https://openrouter.ai/keys 获取 API Key
VITE_OPENROUTER_API_KEY=your_openrouter_api_key

# 图像生成模型（可选，默认使用 google/gemini-2.5-flash-image）
VITE_OPENROUTER_IMAGE_MODEL=google/gemini-2.0-flash-exp

# ============================================
# 以下为可选配置
# ============================================

# API 代理 URL (可选 - 仅在使用 Cloudflare Worker 代理模式时需要)
# 部署 Worker 后会获得此 URL，格式如: https://soul-canvas-api.your-subdomain.workers.dev
# VITE_API_PROXY_URL=http://localhost:8787

# ============================================
# 数据库配置 (Neon PostgreSQL)
# ============================================

# Neon 数据库连接字符串（前端使用）
# 前往 https://neon.tech 创建数据库并获取连接字符串
VITE_DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# 数据库连接字符串（用于 drizzle-kit CLI，不需要 VITE_ 前缀）
DATABASE_URL=postgresql://user:password@host/database?sslmode=require